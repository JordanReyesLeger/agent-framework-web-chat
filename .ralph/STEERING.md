# STEERING — trabajo crítico

Este archivo es el único canal para **intervenir el loop sin detenerlo**.

El agente lo lee al inicio de **cada** iteración. Si hay algo bajo `## Pendiente`,
lo atiende primero y se salta la cola de tareas de esa vuelta.

Úsalo cuando veas, desde afuera, que el agente se está yendo por un camino equivocado.
Edítalo, guarda, y la siguiente iteración lo recoge. No hace falta parar nada.

---

## Pendiente

<!--
Escribe aquí instrucciones concretas y accionables. Ejemplos:

- Estás usando `any` en todos lados en src/api/. Tipa correctamente esos módulos antes de seguir.
- La conexión a la base de datos debe salir de la variable de entorno DB_CONNECTION, no hardcodeada.
- Deja de tocar el módulo de pagos. Está fuera de alcance de esta corrida.
- TASK-0007 está mal especificada: el endpoint es POST /v2/orders, no PUT.

Borra el contenido de esta sección cuando no haya nada crítico.
-->

## Atendido

<!-- El agente mueve aquí lo que ya resolvió, con la fecha. -->
