# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: af-webchat
metadata:
  template: af-webchat-terraform
infra:
  provider: terraform
  path: ./infra
services:
  web:
    project: ./02-AFWebChat
    host: appservice
    language: dotnet
